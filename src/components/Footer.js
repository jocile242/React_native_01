import React from 'react';
import {
    Text,
    View
} from 'react-native';

function Footer() {
    return (
        <View
            style={{
                flexDirection: 'row',
                height: 60,
                padding: 20,
                fontSize: 40,
            }}>
            <View style={{}}/>
            <Text>Footer</Text>
        </View>
    );
}

export default Footer;